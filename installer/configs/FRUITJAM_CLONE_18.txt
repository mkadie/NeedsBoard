# Fruit Jam clone, ZJY180SN00 1.8" panel.
# `variant` below re-pins the choice on the device itself: deploy.sh
# overwrites hardware_config.py but preserves config.txt, so this is what
# survives a later code update.
variant = FRUITJAM_CLONE_18

sleep_enabled = true
sleep_timeout = 120
volume = 80
playback_speed = 100
debounce_time = 0.5
encoder_direction_flip = false
display_hint_text = true
start_menu = base_fruitjam.menu
emergency_push_enabled = true
emergency_push_sound = /button_sounds/emergency.mp3
emergency_hold_enabled = true
emergency_hold_seconds = 3
